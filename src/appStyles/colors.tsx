
// colors used in the app

const Colors = {
    blackOpacity: (opacity: number) => `rgba(0,0,0,${opacity})`,
    primaryColor: "#F6F6F6",
    white: "#fff",
    darkGrey: "#3C3939"

}

export default Colors;