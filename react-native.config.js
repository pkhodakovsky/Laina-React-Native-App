module.exports = {
  project: {
    ios: {},
    android: {}, // grouped into "project"
  },
  assets: ["./src/assets/fonts"], // stays the same
  dependencies: {
    // "react-native-pusher-push-notifications": {
    //   platforms: {
    //     android: null,
    //     // ios: null // this skips autolink for android
    //   }
    // }
  }
};